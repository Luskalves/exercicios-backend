import { expect } from 'chai';
import { describe } from 'node:test';
import Pedido from '../lanchonete';


describe('testando', () => {
  describe('testando o Pedido', () => {
    expect(Pedido).to.be.ok
  })
})